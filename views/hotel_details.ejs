<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= hotel.name %> - Details</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/styles/hotel_details.css">
</head>
<body>
    
    <%- include('partials/navbar_guest') %>

    <div class="container my-5">
        <div class="hotel-details">
            <div class="row">
                <div class="col-md-6">
                    <img src="<%= hotel.image_url %>" alt="<%= hotel.name %>">
                </div>
                <div class="col-md-6">
                    <h1><%= hotel.name %></h1>
                    <p><strong>Location:</strong> <%= hotel.location %></p>
                    <p><strong>Address:</strong> <%= hotel.address %></p>
                    <p><strong>Price per night:</strong> $<%= hotel.price_per_night %></p>
                    <p><strong>Total rooms:</strong> <%= hotel.total_rooms %></p>
                    <p><strong>Facilities:</strong> <%= hotel.facilities %></p>
                    <p><strong>Rating:</strong> <%= hotel.rating %> / 5 (<%= hotel.reviews_count %> reviews)</p>
                    <p><strong>Check-in Time:</strong> <%= hotel.checkin_time %></p>
                    <p><strong>Check-out Time:</strong> <%= hotel.checkout_time %></p>
                    <p><strong>Contact Email:</strong> <%= hotel.contact_email %></p>
                    <p><strong>Contact Phone:</strong> <%= hotel.contact_phone %></p>
                    <p><strong>Description:</strong></p>
                    <p><%= hotel.description %></p>
                    <a href="/book/<%= hotel.id %>" class="btn btn-primary me-2">Book Now</a>
                    <a href="/search-results" class="btn btn-outline-primary">Back</a>
                </div>
            </div>
        </div>
    </div>
    
    <%- include('partials/footer') %>

</body>
</html>
